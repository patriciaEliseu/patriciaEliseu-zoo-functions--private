const { species } = require('../data/zoo_data');
const data = require('../data/zoo_data');

function countAnimals(...animal) {
  const constarEspecies = animal.forEach(spcecies(residents[name]))
  if (!animal) {
    
  }
}

module.exports = countAnimals;
